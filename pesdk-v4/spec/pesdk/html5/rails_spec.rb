require 'spec_helper'

describe Pesdk::Html5::Rails do
  it 'has a version number' do
    expect(Pesdk::Html5::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
